/**
 * Empty Haxe compiler entrypoint retained only for the current `-main` HXML
 * contract. Blog behavior lives in module functions and annotated Next owners;
 * this class has no application or runtime identity.
 */
class BlogMain {
	public static function main():Void {}
}
