package special_files.module_negative.multiple_boundaries;

import genes.react.Element;

@:next.loading("negative/module-boundaries")
@:next.notFound("negative/module-boundaries")
function render():Element {
	return <p>invalid</p>;
}
