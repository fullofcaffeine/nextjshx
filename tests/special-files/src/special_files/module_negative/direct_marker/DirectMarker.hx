package special_files.module_negative.direct_marker;

import genes.react.Element;

@:genes.moduleFunction("render")
@:next.notFound("negative/module-direct-marker")
function render():Element {
	return <p>invalid</p>;
}
