# Proxy fixture

This fixture proves the semantic `@:next.proxy` declaration, compile-time
matcher literals, exact generated adapter plan, and strict genes-ts output.
Negative cases keep malformed proxy signatures and configuration out of the
renderer and out of Next's build.
