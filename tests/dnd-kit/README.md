# dnd-kit integration fixture

This focused fixture proves the reviewed `@dnd-kit/react` and
`@dnd-kit/helpers` boundary independently from the Todo application.

It covers the semantic string-ID Hook, the faithful numeric-ID raw Hook,
intrinsic callback refs, provider callbacks, exhaustive reorder handling,
canonical generated imports, strict TypeScript against the installed public
declarations, official React lint over the genuine module functions generated
from the positive Haxe Hooks, a conditional native TSX negative control,
deterministic output, and exact Haxe negatives for IDs, indices, callbacks, ref
values, ref targets, and Hook placement.

Run it with:

```sh
npm run test:dnd-kit
```
