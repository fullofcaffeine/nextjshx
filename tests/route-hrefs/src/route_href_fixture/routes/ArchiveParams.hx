package route_href_fixture.routes;

import genes.ts.Undefinable;

/** Named Haxe shape surfaced by the generated optional catch-all companion. */
@:structInit
class ArchiveParams {
	public final slug:Undefinable<Array<String>>;

	public inline function new(slug:Undefinable<Array<String>>) {
		this.slug = slug;
	}
}
