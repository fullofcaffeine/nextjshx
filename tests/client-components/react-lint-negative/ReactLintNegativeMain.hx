import client_components_react_lint_negative.GeneratedMissingDependency;

/** DCE root for the generated official-React-lint negative control. */
class ReactLintNegativeMain {
	static function main():Void {
		final hook = GeneratedMissingDependency.useMissing;
		if (hook == null) {
			throw "unreachable";
		}
	}
}
